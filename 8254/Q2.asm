CODE SEGMENT
	ASSUME CS:CODE
START:
	MOV DX, 283H
	MOV AL, 00110100B
	OUT DX, AL

	MOV DX, 280H
	MOV AX, 1000
	OUT DX, AL
	MOV AL, AH
	OUT DX, AL
	
	MOV AH, 4CH
	INT 21H
CODE ENDS 
	END START